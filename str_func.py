def upper_func(string: str) -> str:
    """Функция возвращает строку в виде заглавных букв."""
    text = string.upper()
    return text


def title_func(string: str) -> str:
    """Функция возвращает строку, где каждое слово с заглавной буквы."""
    text = string.title()
    return text
